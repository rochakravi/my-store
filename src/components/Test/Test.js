import React from 'react';
import PropTypes from 'prop-types';

const Test = props => {
    return (
        <div style={{background:'#eca0a063'}}>
            Test
        </div>
    );
};

Test.propTypes = {
    
};

export default Test;